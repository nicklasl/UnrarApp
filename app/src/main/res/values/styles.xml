<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Light">
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/Button</item>
        <item name="android:progressBarStyle">@style/ProgressBar</item>
    </style>

    <style name="Heading" parent="AppTheme">
        <item name="android:textAllCaps">true</item>
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:layout_marginTop">10dp</item>
    </style>

    <style name="Button" parent="AppTheme">
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:minHeight">50dp</item>
    </style>

    <style name="ProgressBar" parent="AppTheme">

    </style>

    <style name="Dotted" parent="android:Widget.TextView">
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:padding">5dp</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:background">@drawable/dotted</item>
    </style>

    <style name="Loader" parent="android:Widget.Material.ProgressBar">
        <item name="android:indeterminate">true</item>
    </style>

</resources>
